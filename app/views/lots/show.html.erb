<div class="container">
  <h2 class="mt-3 mb-3">
    <%= "#{@lot.name}" %>
  </h2>
  <div class="moving-lot">
    <%= button_to "déplacer le lot", edit_lot_path(@lot), method:"get", class:"btn-success" %>
  </div>
  <% @sheeps.each do |sheep| %>
  <%= button_to "mouton n°#{sheep.electronic_id}", sheep_path(sheep.id), method:"get", class:"main-btns" %>
  <%= render "shared/gender_sticker", sheep: sheep %>
  <% end %>
</div>
