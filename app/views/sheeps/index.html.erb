<div class="container">
  <h1>Tous les moutons</h1>
  <% @sheeps.each do |sheep|%>
    <%= button_to "mouton n°#{sheep.electronic_id}", sheep_path(sheep.id), :id => "#{sheep.id}", method:"get", class:"main-btns" %>
    <%= render "shared/gender_sticker", sheep: sheep %>
  <% end %>
</div>
